import 'package:flutter/widgets.dart';

const shadowFloatButton = BoxShadow(
  color: Color.fromRGBO(0, 180, 216, 0.3),
  blurRadius: 10,
  offset: Offset(0, 2),
);

const shadowMenu = BoxShadow(
  color: Color.fromRGBO(160, 146, 136, 0.32),
  blurRadius: 8,
);

const shadowDefault = BoxShadow(
  color: Color.fromRGBO(189, 172, 160, 0.26),
  blurRadius: 10,
);